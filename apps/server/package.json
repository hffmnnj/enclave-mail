{
  "name": "apps/server",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@enclave/crypto": "workspace:*",
    "@enclave/db": "workspace:*",
    "@enclave/types": "workspace:*",
    "bullmq": "^5.0.0",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.0.0",
    "ioredis": "^5.0.0",
    "mailauth": "^4.13.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "bun-types": "^1.3.9"
  }
}
